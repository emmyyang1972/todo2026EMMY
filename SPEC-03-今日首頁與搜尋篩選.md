# SPEC-03 今日首頁與搜尋篩選

## 1. 文件資訊

- 版本：0.1
- 日期：2026-09-14
- 狀態：已確認，可開發
- 相依：SPEC-01、SPEC-02

## 2. 概述

建立登入後預設進入的今日首頁，協助使用者快速知道今天該做什麼，並提供全域任務搜尋、篩選與排序。

## 3. 範圍

### 包含

- 今日焦點區
- 逾期區
- 今日到期區
- 即將到期區
- 進行中區
- 快速新增
- 全域搜尋
- 多條件篩選與排序

### 明確不做

- 自動排程建議
- Email／推播
- 行事曆時間格
- 文獻研究工作台的完整搜尋、摘要與研究方向產生（改由 SPEC-05 定義）

## 4. 技術環境與約束

- 登入後預設路由為 `/today`。
- 所有日期判斷以使用者時區為準，第一期預設 Asia/Taipei。
- 「逾期」優先於「今日到期」顯示，避免同一任務重複出現。

## 5. 相依與執行順序

1. 建立日期與狀態查詢服務
2. 實作今日首頁區塊
3. 實作全域搜尋
4. 實作篩選、排序與 URL query state
5. 補上空狀態、載入與錯誤狀態

## 6. 資料模型

不新增核心表。使用 tasks、projects、tags 的索引與查詢：owner_id、project_id、status、priority、due_at、is_today_focus。

## 7. 角色與權限

沿用 SPEC-01／02，僅顯示目前使用者可存取的任務。

## 8. 任務清單

- 實作今日日期計算與使用者時區
- 實作今日焦點、逾期、今日到期、即將到期、進行中查詢
- 實作任務卡片快速改狀態／優先級／今日焦點
- 實作跨專案搜尋
- 實作狀態、優先級、標籤、專案、日期範圍篩選
- 實作建立時間、截止時間、優先級排序
- 實作無資料、載入中、查詢失敗畫面

## 9. 驗收標準與非功能

- 使用者登入後第一個看到 `/today`。
- 今日焦點只顯示 is_today_focus=true 且未完成／未取消的任務。
- due_at 早於今日開始時間的任務顯示為逾期。
- 今天到期任務不會同時出現在即將到期區。
- 搜尋可跨專案比對任務名稱、說明與標籤。
- 篩選條件可同時套用，重新整理後條件仍可由 URL 還原。
- 空狀態清楚告知使用者下一個可執行動作。

## 10. 待確認／給 Agent 的執行指示

- 「即將到期」第一期定義為未來 7 個曆日內。
- 今日首頁不自動替使用者改變優先級或狀態。
