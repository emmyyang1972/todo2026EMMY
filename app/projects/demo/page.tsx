'use client'
import Link from 'next/link'
import { ArrowLeft, CheckSquare, CalendarDays, FolderKanban, Inbox, Settings, MoreHorizontal, Plus } from 'lucide-react'
type BoardItem = [title: string, tag: string]
type BoardGroup = [name: string, items: BoardItem[]]

const groups: BoardGroup[]=[['待處理',[['回覆設計師的修改建議','一般'],['準備競品分析','低']]],['進行中',[['整理產品需求文件','高']]],['阻塞',[['完成首頁 wireframe','重要']]],['已完成',[['訪談使用者並整理筆記','']]]]
export default function Project(){return <div className="shell"><aside className="sidebar"><div className="brand"><span className="brand-mark"><CheckSquare size={19}/></span><span>FocusDesk</span></div><p className="nav-title">工作空間</p><Link className="nav-item" href="/today"><CalendarDays size={17}/><span>今日</span></Link><Link className="nav-item active" href="/projects"><FolderKanban size={17}/><span>專案</span></Link><Link className="nav-item" href="/inbox"><Inbox size={17}/><span>收件匣</span></Link><div className="side-bottom"><Link className="nav-item" href="/settings"><Settings size={17}/><span>設定</span></Link></div></aside><main className="main"><Link href="/projects" className="muted"><ArrowLeft size={15} style={{verticalAlign:'-3px'}}/> 返回專案</Link><div className="topline" style={{marginTop:25}}><div><h1>產品網站改版</h1><div className="date">進行中 · 截止日期 2026年9月30日 · 3/8 項完成</div></div><button className="google-btn" style={{width:'auto',padding:'11px 16px'}}><Plus size={16}/> 新增任務</button></div><div className="progress" style={{marginTop:25}}><i style={{width:'38%'}}/></div><div className="board">{groups.map(([name,items])=><div className="column" key={name}><h3>{name} <span className="muted">{(items as string[][]).length}</span></h3>{(items as string[][]).map(([title,tag])=><div className="task" key={title}><div className="task-title">{title}</div>{tag&&<span className="pill">{tag}</span>}<div className="muted" style={{marginTop:10}}><MoreHorizontal size={15}/></div></div>)}</div>)}</div></main></div>}
