# SPEC-05 文獻研究工作台

## 1. 文件資訊

- 版本：0.1
- 日期：2026-09-17
- 狀態：已確認，可開發
- 相依：SPEC-01、SPEC-02、SPEC-03

## 2. 目標

將 FocusDesk 從一般待辦工具擴充為論文文獻研究工作台，讓使用者每天可以：

1. 搜尋與自己研究主題相關的最新文獻。
2. 對每篇文獻整理可回溯原文的研究重點。
3. 從文獻集合產生可進一步評估的論文研究方向。

## 3. 第一版範圍

### 包含

- `/literature` 文獻研究頁面。
- 使用者輸入關鍵字，從 PubMed 搜尋最近 1、7、30 或 90 日文獻。
- 顯示標題、期刊／年份、PMID、摘要與 PubMed 原文連結。
- 將摘要整理成研究目的、方法／資料、結果／結論等重點；無摘要時明確標示「需人工閱讀」。
- 依目前搜尋結果列出最多 5 個研究方向，每個方向包含研究問題、可能設計、可觀察結果與需補查的缺口。
- 將當日搜尋結果與查詢詞暫存於瀏覽器，依日期回看；未設定 Supabase 時仍可使用。
- 從今日首頁連到文獻研究工作台。

### 明確不做

- 不把規則式摘要宣稱為正式系統性回顧或人工審查結果。
- 不自動下載、上傳或改寫原始論文檔案。
- 不自動代替使用者決定論文題目、研究倫理或統計方法。
- 第一版不做背景 cron；每日搜尋由使用者開啟頁面並執行，可在後續接排程服務。

## 4. 資料來源與技術約束

- 第一版文獻來源：NCBI PubMed E-utilities（ESearch + EFetch）。
- 搜尋必須保留 PMID 與 PubMed URL，所有整理內容都要能回到來源摘要或原文。
- 查詢字串、最近天數與筆數由 UI 傳入；筆數上限 20，避免過度呼叫公開服務。
- API 只在伺服器端呼叫外部服務；瀏覽器不得暴露服務端密鑰。
- 使用者時區沿用 Asia/Taipei；搜尋日期以當地日期記錄。
- NCBI 失敗、逾時或摘要缺失時，顯示可理解錯誤與人工處理提示，不得捏造內容。

## 5. 研究方向產生規則

研究方向是「研究候選」，不是確定建議。系統從搜尋結果的標題與摘要辨識重複主題，產生：

- 候選研究問題：目前證據在哪個場域、族群或介入仍不足。
- 可能研究設計：依文獻線索提出橫斷面、前後比較、混合方法或系統性回顧等候選。
- 主要結果指標：品質、安全、成本、碳排、流程時間或病人／員工結果等。
- 需人工補查：樣本、偏差、追蹤期、倫理與在地可行性。

## 6. 驗收標準

- 輸入有效關鍵字並搜尋後，頁面顯示最多 20 篇 PubMed 文獻及其 PMID／連結。
- 每篇文獻都有來源資訊、摘要文字與至少一段可辨識的重點整理；無摘要時清楚標記。
- 搜尋結果可用「儲存今日整理」保留，重新整理後仍可查看當日紀錄。
- 搜尋結果區塊會顯示至少一個研究方向，並明確標示為候選、需要人工驗證。
- 搜尋失敗時不清空既有結果，且顯示錯誤原因與重試操作。
- `npx.cmd tsc --noEmit`、`npm.cmd run build` 通過，`/literature` HTTP 200。

## 7. 後續版本

- Supabase `literature_searches`、`literature_items`、`research_ideas` 資料表與 RLS。
- Crossref、Europe PMC 或學校訂閱資料庫來源。
- 使用者確認後的人工筆記、引用格式、去重與標籤。
- 背景每日排程、Email／推播與可選的合規 AI 摘要服務。
